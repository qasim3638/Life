# Life Blueprint — PRD

## Original Problem Statement
Build a motivational/life-planning app to help a 40-year-old shape the next 40 years:
fitness (customizable workouts), healthy recipes (Pakistani/Indian/Arab, low-carb high-protein, no pork/bacon),
self-love & self-care, motivational podcasts/speeches/quotes, meditations, personal dates/events/reminders,
and anything that helps build a routine for staying healthy, happy and fit.

## User Choices
- AI via **Claude Sonnet 4.5** (Emergent LLM key)
- **No auth** — single-user local app
- Motivation = curated quotes + YouTube embeds
- In-app reminders only
- Design vibe: **Warm & calm** — Cormorant Garamond + Manrope, #FDFBF7 / #59745D moss / #C27A62 terracotta / #A3897C clay / #E8E2D2 sand

## Architecture
- Backend: FastAPI + MongoDB (Motor), modular routes under `/app/backend/routes/`
  (`workouts, recipes, journal, events, life_goals, content, day_plans, streaks, ai_endpoints, companion, family, audio, self_profile, focus, sobriety, echo, sunday_review, uploads`)
- Static files served at `/api/uploads/*` from `/app/backend/uploads/`
- AI: `emergentintegrations` → Claude Sonnet 4.5 (`claude-sonnet-4-5-20250929`)
- Frontend: React + shadcn/ui + Tailwind. Pages in `/app/frontend/src/pages/`, shared card components under `/app/frontend/src/components/{today,companion}/`.

## Features Shipped
1. **Today** – life-arc ring, AI wisdom, quote, echo-of-yesterday, protected (sober+focus) card, daily AI brief, streak protector (after 6pm), Sunday banner, weekly letter. Cards extracted into `/components/today/*` for maintainability.
2. **Tomorrow** – time-blocked schedule, morning routine, meals, supplements, chores
3. **Blueprint** – 40-year goals + jsPDF export
4. **Fitness** – workout builder, logs, AI coach
5. **Recipes** – 12 seeded halal low-carb high-protein recipes, filters, AI chef, **file-upload OR URL-paste for recipe images** (stored in `/app/backend/uploads/`, max 5MB, .jpg/.jpeg/.png/.webp/.gif)
6. **Motivation** – quotes + YouTube podcasts/speeches
7. **Meditate** – breath timer + global sticky audio player (meditation music, sleep stories, wisdom stories)
8. **Self-Care** – mood, gratitude, journal, affirmations, AI coach
9. **Events** – shadcn Calendar, recurring dates/reminders
10. **Self Profile** – appearance/personality/mind/style/gear + AI Daily Brief
11. **Focus** – Pomodoro sessions + distraction logging, tz-aware stats
12. **Sobriety** – multi-addiction tracker, clean streak, slip logging
13. **Family** – timeline of memories + holidays, edit/delete
14. **Companion** – AI friend with auto-memory extraction, pinning, TTL. Sidebar/dialogs extracted into `/components/companion/*`.
15. **Sunday Rhythm Review** – weekly AI reflection pulled from workouts/journal/focus/slips/family/plan data. Featured card + past reviews with 5 stat tiles. Sunday-only banner on Today dashboard.

## Changelog
- 2026-02: Sunday Rhythm Review frontend (Review.jsx + nav + Today banner). 102/102 backend tests + frontend E2E pass.
- 2026-02: Recipe image upload endpoint + file-picker/URL UI. Today.jsx refactored into 5 cards (316 lines, was 461). Companion.jsx refactored into 3 extracted components. 100% backend + frontend tests.
- 2026-02: Upload endpoint now auto-compresses to WebP — full-size (longest edge ≤ 1600px, q85) + 400×300 thumbnail (q75). Recipe model gained `thumb` field; card grid uses thumbnail, detail modal keeps full. 2400×1600 JPEG (60KB) → 3KB WebP + 300-byte thumb (~20× smaller).
- 2026-02: Recipe cards now use native `<img>` with `loading="lazy"`, `decoding="async"`, and fixed `width=400 height=300` (modal: 800×448) for zero-CLS, smooth scrolling at any catalog size.
- 2026-02: AI macro estimation — `POST /api/ai/recipe-macros` uses Claude Sonnet 4.5 to estimate prep time + calories + protein/carbs/fat per serving from the ingredient list. New "Estimate macros" button in the Add Recipe dialog pre-fills all five numeric fields. Backend + frontend tests 100%.
- 2026-02: YouTube library curation — `POST/DELETE /api/podcasts` and `/api/meditations` accept any YouTube URL form (watch?v=, youtu.be, /embed/, /shorts/, or raw 11-char ID), extract the ID server-side via regex, and store as `is_custom: true`. Motivation + Meditate pages have an "Add your own" dialog. Seeded items are protected from deletion (404 returned). Resilient UI: `<YouTubeThumb>` auto-detects YouTube's 120×90 broken-video placeholder and swaps in a warm fallback card; every card carries a "Watch on YouTube ↗" escape-hatch link. Fixed 5 dead seeded IDs (Jordan Peterson, Goggins, Eckhart Tolle, Rumi, Walking Meditation) in seed file + live DB.
- 2026-02: Removed "Made with Emergent" badge from `public/index.html`.
- 2026-02: Shuffle / "Try another" — per-card shuffle button on every Motivation podcast and Meditate audio card cycles that slot to a random other video from the library.
- 2026-02: Library expansion — 8→17 podcasts, 6→15 meditations, 15→40 quotes. All YouTube IDs HTTP-verified.
- 2026-02: "Fresh batch" quote refresh button — displays 6 random quotes at a time; rotates to a new random sextet on click (respects active category filter).
- 2026-02: Companion → actions — Claude returns a strict JSON envelope `{reply, actions[]}` when the user asks for something actionable. 4 action types supported: `add_time_block`, `add_event`, `add_priority`, `add_chore`. Each proposed action renders as a beige-bordered chip with an icon + human-readable summary + Apply/Cancel buttons beneath the assistant message. Apply writes to the corresponding collection (day_plans, events) and marks the chip APPLIED (green). Cancel marks SKIPPED (grey, line-through). Apply is idempotent (second call returns `{ok, already: 'applied'}`). Backend `/api/companion/messages/{mid}/actions/{aid}/apply` and `/cancel`. Backend 6/6 + frontend 3/3 tests pass.
- 2026-02: Companion → live weather tool — Open-Meteo integration (no API key). `POST /api/companion/location` resolves city name via geocoding (tolerant of country suffixes). When user asks weather-related questions, current + 3-day forecast is injected into the Claude prompt. New "Your city (for live weather)" field in Companion settings. Companion model gained `location_name`, `latitude`, `longitude` fields.
- 2026-02: Companion reply depth — loosened prompt so Claude answers factual questions with real substance (200-400 word answers on Stoicism, protein sources over 40, etc.) instead of generic hedging. Still respects brevity for chit-chat.
- 2026-02: Sanctuary page (`/sanctuary`) — new calming space with 3 tabs: **Sounds** (16 curated long-form ambients — rain, ocean, forest, thunderstorm, fire), **Scenery** (8 cinematic 4K nature films — Norway 12h, BBC Earth, Splendors of Nature), **Stills** (24 Unsplash photos — mountain lakes, forests, dunes). Per-card shuffle, Add-your-own via AddYouTubeDialog, YouTube fallback-safe thumbnails, player embeds inline. Backend `/api/sanctuary/{sounds,scenery,stills}` with seed-if-empty on startup.
- 2026-02: Companion **maximum tools** pass:
  - **App-awareness**: when the user asks about themselves/their life (keyword heuristic), companion_context.py builds a compact summary of today's + tomorrow's plan, upcoming 14-day events, last-7-day workouts/journal moods/focus sessions, sobriety streaks, and self-profile, and injects it. Results: companion now gives genuinely personal, specific analysis referring to actual logged data.
  - **Prayer times**: AlAdhan API (free, no key). Triggered on prayer-intent keywords. Uses saved location.
  - **World clock**: stdlib `zoneinfo` with 40+ city aliases (Karachi, Dubai, Tokyo, etc.), fallback to IANA substring match.
  - **Unit conversion**: mass/volume/length + temperature (C/F/K). Hand-written, no API.
  - **New action types**: `log_workout` (name + duration_min + date + intensity), `log_journal` (mood 1-5 + gratitude + entry + date). Validates, persists, frontend chip renders with Dumbbell/HeartPulse icons.
- 2026-02: Today hero wallpaper — daily-rotating random nature still from Sanctuary stills (seeded by date so it's consistent for the full day). Replaces static background.
- 2026-02: Proactive Companion alerts on Today — rule-based, zero AI latency. 7 alert types: workout-gap (4+ days), journal-gap (3+ days), prayer-coming (≤30 min), rain-today (≥60% chance), event-tomorrow, Sunday-reflection, plan-drift (no priorities by afternoon). Up to 4 shown, sorted by urgency; dismissible (per-day via localStorage). Colour-toned by theme (practical/nudge/soft/spiritual). Backend `GET /api/companion/alerts`.
- 2026-02: **Sanctuary Mode (immersive player)** — full-screen distraction-free mode for sounds (photo-still backdrop with slow drift + hidden audio loop + mute toggle), scenery (video fills screen), and stills (lightbox with prev/next/shuffle). Keyboard navigation: ESC to exit, ←/→ to navigate, M to mute (sounds). Body scroll locked while immersive. Per-card Maximize2 button + "Sanctuary mode" CTA on the inline player + "Enter Sanctuary" green button on Stills tab. Backdrop derived deterministically from item id. New component `/components/sanctuary/SanctuaryMode.jsx`, CSS animations in `index.css` (sanctuary-fade-in, sanctuary-fade-in-up, sanctuary-drift). Smoke-tested: open + ESC close + stills entry all work.
- 2026-02: **Companion chat persistence + recall hardened** — chat history was already persisted in MongoDB but not strongly recalled. Bumped injected history from last 12 → last 24 messages (fetched 40), reframed the transcript block as `=== CONVERSATION SO FAR (verbatim transcript) ===` with explicit "treat as authoritative ground truth" instruction. Verified end-to-end: stating a fact (Lake District) → topic switch to Sapiens → "what did I say about my favourite holiday spot?" → Najm correctly recalls "The Lake District in Cumbria — you mentioned it at the start of our conversation today."
- 2026-02: **`addMemory` 404 error overlay fix** — wrapped `addMemory`, `removeMemory`, `togglePin`, `rememberMessage`, `clearChat` in try/catch with toast feedback so transient network blips no longer trigger CRA's red runtime-error overlay. Endpoint itself was already healthy (HTTP 200); this prevents the unhandled-rejection crash UX.
- 2026-02: **Chat persistence indicator** — Companion header now shows "· N msgs kept" next to persona label with a tooltip explaining history is saved and your companion uses recent messages + memories to recall topics.
- 2026-02: **Memory Lane** — searchable view over the entire chat transcript. New `GET /api/companion/messages/search?q=...` returns matched messages with one-message-before and one-message-after for context. Frontend `MemoryLaneDialog.jsx` mounted on Companion page (Search icon button in header). Debounced (320ms) search, yellow highlight on matched substring, "You / {companion.name}" speaker labels, date+time stamps, and italic context lines top & bottom. Verified with "Cumbria" search → 3 hits across user statement, Yaar's confirmation, and Yaar's later recall. Companion role label in injected transcript also now uses dynamic `companion.name` (post user-rename to "Yaar").
- 2026-02: **Memory Lane → unified search across 3 sources**. Same endpoint now also searches saved memories (`companion_memories`) and journal entries (`journal_entries.reflection` + each `gratitude` item). Each result is tagged `kind: chat | memory | journal` with sorted `ts`. Dialog renders 3 distinct cards: Conversation (with neighbour context), Memory (eyebrow shows category + "pinned" if pinned), Journal (with mood label + reflection block + matched gratitude bullets). Verified end-to-end with a journal entry and a memory both mentioning "Cumbria" — 5 mixed hits returned.
- 2026-02: **Yaar PIN lock** — backend stores a bcrypt-hashed PIN on the `companion` doc field `pin_hash` (NEVER returned by `GET /companion`; new helper `_public_companion()` strips it). New endpoints: `GET /api/companion/pin/status`, `POST /api/companion/pin` (set or change with `current_pin`), `POST /api/companion/pin/verify`, `DELETE /api/companion/pin` (with `current_pin`). PIN is 4-8 digits. Frontend: new `PinGate.jsx` component shown when `pin_status.enabled && !sessionStorage.yaar_unlocked_at`. 30-min unlock window via `sessionStorage`. New "Lock" icon button in chat header lets you re-lock instantly. Wrong PIN → "Incorrect PIN" inline error. PIN can be set/changed/removed from Settings dialog (new "Privacy" section). Companion data (`/companion/messages`, `/companion/memories`) is only fetched after unlock. Verified end-to-end via screenshots: gate appears → wrong PIN errors → correct PIN unlocks chat + shows lock button.
- 2026-02: **Today's plan card** — new `TodayPlanCard.jsx` mounted as the prominent first card of the Today bento (full-width, lg:col-span-3). Reads/writes `GET/PUT /api/day-plans/{today}`. Shows: top 3 priorities (text input + circular tickbox; tick ⇒ strikethrough + "Done · 8:43 AM" timestamp), 3 chore columns (Morning routine ☀️, House 🏠, Work 💼) each with tickboxes + inline-prompt + Add (+) and per-row Remove (×) on hover, gym status badge, and a top-right progress counter (`5 / 10`) plus a smooth gradient progress bar. New `PriorityStatus` Pydantic model (parallel to existing `priorities: List[str]` for backwards compat) tracks `done` + `completed_at` per slot. Older day_plan docs are auto-backfilled with empty status on GET. Verified end-to-end: ticking priority 1 strikes through the text and shows "Done · 8:43 AM"; progress bar updates instantly; chores persist across reloads.
- 2026-02: **Yaar → the whole app** — massive expansion of Companion action coverage. 8 new action types: `tick_priority` (by index or fuzzy text match), `tick_chore` (kind + fuzzy text), `set_meal` (breakfast/lunch/dinner/snack), `add_supplement`, `add_gratitude`, `log_mood`, `add_life_goal` (Blueprint), `add_family_memory`. All enforced by extended `validate_action()` and executed by `execute_action()` in `companion_actions.py`. Prompt updated to teach Claude the new shapes + examples. Fixed a Claude-quirk date bug: Claude was emitting 2025 dates despite being told it's 2026 (training cutoff bias) — backend now snaps any date >30d in the past or >365d future back to today automatically via `_fix_date()`. Prompt was also strengthened with `!!! CRITICAL — TODAY'S DATE IS {today_iso} !!!` banner. Verified end-to-end: one chat turn saying "Mark the gym priority done, tick off the bins chore, set lunch to chicken salad, add creatine to supplements, log my mood as 4, and add a gratitude for my morning coffee" → **6 actions proposed, all 6 applied, all 6 DB writes verified** (priority ticked with timestamp, chore ticked, meal set, supplement added, mood 4 logged, gratitude appended).
- 2026-02: **Inline "Ask Yaar" composer on Today** — new `AskYaarInline.jsx` mounted inside the Today's plan card (reusable, accepts placeholder/hint/onApplied). Sends the message through `/companion/chat`, auto-applies every proposed action, shows a green checklist of what was changed, and calls the parent's `onApplied` so the card reloads instantly. No page hop, no Apply/Cancel chips — just type "add pick up dry cleaning to house chores" → the row appears in the House column within 3 seconds. Verified: chore count went 1 → 2, applied-message read "Added house chore: pick up dry cleaning".
- 2026-02: **ActionChips UI updated** — icons + human-readable summaries for all 8 new action types (CheckCircle2, UtensilsCrossed, Pill, Sparkles, Smile, Target, BookHeart). Mood action renders with word label ("4/5 — good").
- 2026-02: **Floating "talk to Yaar" mic** — `VoiceMicButton.jsx` mounted app-wide (fixed bottom-right, z-91). New backend route `POST /api/voice/transcribe` (accepts `multipart/form-data` audio blob, hands to OpenAI Whisper-1 via `emergentintegrations.OpenAISpeechToText`, returns `{text}`). Frontend uses `MediaRecorder` API with codec autodetection (webm/opus → mp4 → ogg fallback), supports both **single-tap-to-toggle** and **press-and-hold** (≥350ms threshold) interactions. Pipeline: record → transcribe via Whisper → forward transcript to `/companion/chat` → auto-apply every proposed action → toast + status panel showing transcript + green-check applied summaries. Visual states (idle/recording/transcribing/thinking/done) with pulsing red ring while recording, spinner while transcribing, green check on success. Auto-clears 6s after done. 24MB upload cap, graceful permission-denied + unsupported-browser fallback. Verified end-to-end: backend whisper-1 transcription returned 200 with `{text}`; mic button rendered on Today/Tomorrow/Companion.
- 2026-02: **Yaar speaks back (TTS)** — new `POST /api/voice/speak` endpoint streams MP3 (`audio/mpeg`) via OpenAI `tts-1` + `coral` voice (warm, friendly) using `emergentintegrations.OpenAITextToSpeech`. After voice-command pipeline applies actions, frontend builds a concise spoken summary ("Done. Gym ticked. Lunch set: chicken salad.") and plays it via `new Audio(blobUrl)`. New mute toggle button (Volume2 / VolumeX icons) sits to the LEFT of the mic at bottom-right; preference persisted in `localStorage["yaar_voice_replies"]`. While speaking, the toggle pulses with a green ring AND doubles as a "stop voice" button. Status panel auto-clear extends to 12s when Yaar is speaking back so the message stays visible. Verified: backend returned 43KB MP3 audio for "Done. Gym ticked..."; mute toggle renders, click toggles to "Yaar will stay quiet" and icon flips to VolumeX; persists across reloads via localStorage.
- 2026-02: **Proactive voice briefs** — Yaar speaks scheduled summaries throughout the day. New backend `POST /api/voice/brief` endpoint (`{kind, custom_prompt?}`) generates a deterministic spoken paragraph for `morning|midday|evening` based on live DB data (priorities + status, chore progress, events, mood, journal-gap nudges, tomorrow's first move) — fast, predictable, no LLM cost. `kind=custom` delegates to the companion chat pipeline for AI-driven personalised briefs. Verified outputs: morning *"Good morning, Qasim. Today is Tuesday the 5th of May. You've got 2 priorities..."*; midday *"You've finished 1 of 2 priorities. Still on the list: Reply to John email."*; evening *"You logged today as good. Sleep well."* New helper `voice_briefs.py` with date/time formatters.
- 2026-02: **Brief scheduler + settings dialog** — `BriefScheduler.jsx` runs invisible app-wide, ticks every 30s, fires a brief when the user-configured time has been reached AND it hasn't been fired today (per-day `firedToday` map in localStorage with 14-day prune). Two channels: **(a)** in-app toast (top-right, animated) with Play / Later buttons; **(b)** browser Notification (when permission granted AND tab is not visible). **Auto-play smart rule**: if `document.visibilityState === "visible"` AND user has interacted with the page once (so browser allows audio) AND `yaar_voice_replies` is on → audio auto-plays. Otherwise waits for tap. New `BriefSettingsDialog.jsx` (opens from Companion header → "Briefs" button) lets user toggle each brief, set time via `<input type="time">`, edit custom-brief label + prompt, preview each brief on demand, request notification permission, and add/remove unlimited custom briefs. Schedule lives in `localStorage["yaar_brief_schedule_v1"]` with sensible defaults (8am morning on, 1pm midday off, 9pm evening on). Verified end-to-end: settings dialog opens with 3 defaults, Add Custom creates a new row with prompt textarea, preview generates audio + toast + spoken playback, brief toast fires with full text + Play/Later buttons.

## Prioritized Backlog
- **P2** One-tap action chips on Today's proactive alerts (e.g., "Schedule 30min tomorrow" inline button on workout-gap alert)
- **P2** A11y: add `aria-describedby` / DialogDescription to all shadcn Dialogs
- **P3** Drag-to-rearrange time blocks on Tomorrow page
- **P3** Emailable weekly-review keepsake / year-in-review compile
